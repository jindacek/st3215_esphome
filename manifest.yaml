name: st3215_servo
version: 0.1.0

platforms:
  - st3215_servo

build:
  include:
    - components

dependencies:
  esphome:
    components:
      - uart
      - sensor
      - number
      - text_sensor

codeowners:
  - "@jindacek"
