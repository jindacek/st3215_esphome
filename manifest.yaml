name: st3215_servo
version: 0.2.0

build:
  include:
    - components/st3215_servo

platforms:
  - st3215_servo

dependencies:
  esphome:
    components:
      - uart
      - sensor
      - switch
      - button

codeowners:
  - "@jindacek"
